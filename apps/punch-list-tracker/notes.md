# punch-list-tracker — Notes

## 2026-04-13: Initial Discovery

Rebar discover ran. `expertise.yaml` was filled with a domain draft for a greenfield punch-list tracker; refine when stack and code exist.

## Maintenance

After adding application source and manifests (`package.json`, etc.), run **`rebar_discover`** for `punch-list-tracker` again (or edit `expertise.yaml`) so **architecture** and **api_gotchas** stay aligned with the codebase.
